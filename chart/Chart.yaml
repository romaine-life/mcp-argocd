apiVersion: v2
name: mcp-argocd
version: 0.1.0
description: Read-only ArgoCD MCP server. Outbound auth via the auth.romaine.life k8s exchange (projected SA token → role=service JWT → ArgoCD API); incoming auth via kube-rbac-proxy K8s SA TokenReview.
