apiVersion: v1
kind: Service
metadata:
  name: mcp-argocd
  namespace: mcp-argocd
spec:
  type: ClusterIP
  selector:
    app: mcp-argocd
  ports:
    - name: http
      port: 80
      targetPort: proxy
